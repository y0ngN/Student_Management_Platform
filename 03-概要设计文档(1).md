# 概要设计文档（V0.3-RuoYi版）

## 1. 设计目标
基于 RuoYi-Vue 搭建电力负荷与供电需求分析管理平台，围绕五个核心问题（负荷预测、预测时长、需求预测、AGC 提前感知、高相关因素识别）实现可落地的工程闭环：
1. 数据接入与治理。
2. 多任务模型训练与发布。
3. 在线预测与批量预测。
4. 告警联动与报表输出。

## 2. 技术架构

### 2.1 总体架构
- 前端：RuoYi-Vue（Vue + Element UI + ECharts）
- 后端：RuoYi（Spring Boot + Spring Security + MyBatis）
- 数据库：MySQL
- 缓存：Redis
- 调度：RuoYi Quartz
- 模型服务：Python（FastAPI，独立服务）
- 对象存储（可选）：MinIO（模型包与报表文件）

### 2.2 职责边界
- RuoYi 后端：鉴权、菜单权限、业务编排、任务调度、结果持久化、告警管理。
- Python 服务：特征处理、训练、评估、推理、特征重要性计算。
- 前端：看板、任务操作、结果展示、报表导出。

## 3. 模块设计（RuoYi 映射）

### 3.1 复用模块
- `ruoyi-system`：用户、角色、菜单、字典、参数、通知公告。
- `ruoyi-framework`：登录鉴权、操作日志、全局异常。
- `ruoyi-quartz`：训练、批预测、数据同步定时任务。

### 3.2 新增业务模块 `ruoyi-power`
1. `power-data` 数据中心。
2. `power-analysis` 分析中心。
3. `power-model` 模型中心。
4. `power-predict` 预测中心。
5. `power-alarm` 告警中心。

## 4. 业务子系统与核心能力

### 4.1 数据中心
- 文件导入：CSV/Excel 模板化导入。
- 字段映射：源字段 -> 标准字段（timestamp、tag、value）。
- 质量校验：缺失率、重复率、异常值、时间连续性。
- 输出：`power_raw_data`、`power_import_log`、`power_data_quality`。

### 4.2 分析中心
- 趋势分析：分钟/小时/日聚合。
- 相关分析：Pearson/Spearman。
- 先行性分析：交叉相关（AGC 与负荷变化）。
- 输出：高相关因子排名、分析报告快照。

### 4.3 模型中心（四类任务）
1. 负荷预测回归：目标 `load_actual`。
2. 需求预测回归：目标 `load_cmd`（供电需求代理）。
3. AGC 变化预警分类：未来 10/30/60 分钟变化概率。
4. AGC 值预测回归：未来 AGC 连续值。

### 4.4 预测中心
- 在线预测：给定时间点返回多时窗结果（10/30/60/120 分钟）。
- 批量预测：Quartz 定时跑批并落库。
- 结果解释：返回 topN 影响因子。

### 4.5 告警中心
- 告警类型：阈值告警、偏差告警、趋势突变告警。
- 处理流：新建 -> 确认 -> 处置 -> 关闭。
- 留痕：处理人、处理时间、备注。

## 5. 数据流程设计
1. 采集 CSV 入库（原始层）。
2. 标准化处理（按分钟对齐，统一时区与时间格式）。
3. 特征构建（lag、diff、rolling、时间特征）。
4. 训练集构建（按时间切分 train/valid/test）。
5. 模型训练与评估（写入模型版本与指标）。
6. 模型发布（生效版本写配置）。
7. 在线/批量推理（结果入 `power_predict_result`）。
8. 偏差监测触发告警（入 `power_alarm_event`）。

## 6. 特征与标签设计

### 6.1 主键
- `biz_time + unit_id`。

### 6.2 核心特征
- 过程量：`load_actual`、`load_cmd`、`agc_cmd`、`power`。
- 约束量：`load_up`、`load_low`、`rate_limit`。
- 衍生特征：
1. 滞后特征：`lag_1/5/10/30/60`。
2. 变化特征：`diff_1/diff_5`。
3. 滑窗统计：`rolling_mean/std/max/min`。
4. 时间特征：`hour`、`day_of_week`。

### 6.3 标签定义
- 负荷预测：`y_load_h10/h30/h60/h120`。
- 需求预测：`y_demand_h10/h30/h60/h120`。
- AGC 预警：`y_agc_change_h10/h30/h60`（0/1）。

## 7. 接口概要设计

### 7.1 RuoYi 业务接口
- `POST /power/data/import`
- `GET /power/data/quality/list`
- `POST /power/model/train`
- `POST /power/model/publish/{versionId}`
- `GET /power/model/version/list`
- `POST /power/predict/load`
- `POST /power/predict/demand`
- `POST /power/predict/agc`
- `GET /power/predict/result/list`
- `GET /power/alarm/list`
- `PUT /power/alarm/handle`

### 7.2 预测接口请求/响应示例
请求（`POST /power/predict/load`）：
```json
{
  "unitId": "4",
  "predictTime": "2021-08-26 03:30:00",
  "horizons": [10, 30, 60, 120]
}
```

响应：
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "unitId": "4",
    "predictTime": "2021-08-26 03:30:00",
    "results": [
      {"horizon": 10, "value": 165.2, "risk": "low"},
      {"horizon": 30, "value": 168.7, "risk": "low"}
    ],
    "topFactors": ["power_lag1", "load_cmd_lag5", "agc_cmd_diff1"]
  }
}
```

### 7.3 Java -> Python 内部接口
- `POST /ml/train/load`
- `POST /ml/train/demand`
- `POST /ml/train/agc-classify`
- `POST /ml/predict/load`
- `POST /ml/predict/demand`
- `POST /ml/predict/agc`
- `GET /ml/model/health`

## 8. 数据库概要设计（字段级草案）

### 8.1 `power_raw_data`
- `id` bigint PK
- `unit_id` varchar(32)
- `tag_name` varchar(64)
- `biz_time` datetime
- `value` decimal(16,6)
- `source_file` varchar(255)
- `create_time` datetime

### 8.2 `power_feature_data`
- `id` bigint PK
- `unit_id` varchar(32)
- `biz_time` datetime
- `feature_json` json
- `label_json` json
- `dataset_type` varchar(16)（train/valid/test）

### 8.3 `power_model_task`
- `task_id` bigint PK
- `task_type` varchar(32)（load/demand/agc_cls/agc_reg）
- `status` varchar(16)（pending/running/success/failed）
- `start_time` datetime
- `end_time` datetime
- `log_text` text

### 8.4 `power_model_version`
- `version_id` bigint PK
- `task_type` varchar(32)
- `model_name` varchar(64)
- `model_path` varchar(255)
- `metrics_json` json
- `is_active` tinyint
- `create_time` datetime

### 8.5 `power_predict_result`
- `id` bigint PK
- `task_type` varchar(32)
- `unit_id` varchar(32)
- `predict_time` datetime
- `horizon_min` int
- `predict_value` decimal(16,6)
- `actual_value` decimal(16,6)
- `risk_level` varchar(16)
- `factors_json` json

### 8.6 `power_alarm_event`
- `alarm_id` bigint PK
- `alarm_type` varchar(32)
- `alarm_level` varchar(16)
- `alarm_time` datetime
- `content` varchar(500)
- `status` varchar(16)
- `handler` varchar(64)
- `handle_time` datetime

## 9. 权限与菜单设计
- 一级菜单：电力分析平台。
- 二级菜单：数据中心、分析中心、模型中心、预测中心、告警中心。
- 权限编码建议：
1. `power:data:import`
2. `power:data:query`
3. `power:model:train`
4. `power:model:publish`
5. `power:predict:query`
6. `power:alarm:handle`

## 10. 定时任务设计（Quartz）
1. `powerDataSyncJob`：每 5 分钟同步并标准化新数据。
2. `powerFeatureBuildJob`：每小时构建特征快照。
3. `powerModelRetrainJob`：每日凌晨重训（可按周调整）。
4. `powerBatchPredictJob`：每 10 分钟批量预测未来 120 分钟。
5. `powerAlarmCheckJob`：每 10 分钟执行偏差检查并生成告警。

## 11. 模型评估与验收口径
- 负荷预测：重点关注 NMAE、RMSE（10/30/60/120 分钟分窗统计）。
- 需求预测：重点关注 NMAE、RMSE。
- AGC 感知：重点关注 F1、Recall、AUC。
- 因子解释：输出 top10 特征并可视化。

建议课程验收目标：
1. 负荷预测 60 分钟 NMAE <= 8%。
2. 需求预测 60 分钟 NMAE <= 7%。
3. AGC 30 分钟预警 F1 >= 0.70。

## 12. 部署与运维

### 12.1 环境拓扑
- `Nginx` -> `RuoYi-Admin` -> `MySQL/Redis`
- `RuoYi-Admin` -> `Python-ML-Service`

### 12.2 可观测性
- 接口日志：请求耗时、状态码、traceId。
- 任务日志：训练任务、批预测任务执行记录。
- 告警日志：告警生成与处置留痕。

### 12.3 容错策略
- Python 服务调用超时重试（最多 3 次）。
- 模型不可用时回退到上一活跃版本。
- 批任务失败自动告警到平台消息中心。

## 13. 开发迭代建议
1. Sprint1：数据中心 + 基础看板。
2. Sprint2：负荷/需求预测链路打通。
3. Sprint3：AGC 预警 + 告警中心。
4. Sprint4：模型解释、报表导出、验收优化。

## 14. 后续详细设计待补
- 完整 DDL 与索引设计。
- DTO/VO 字段级协议与错误码定义。
- 前端页面原型与交互稿。
- 生产部署参数（连接池、线程池、限流阈值）。
