# 需求文档（V0.2-RuoYi版）

## 1. 文档目的
定义基于 RuoYi 框架建设“电力负荷与供电需求分析管理平台”的业务需求、功能需求和验收标准，作为开发、测试、上线依据。

## 2. 背景与目标
基于老师提供的原始数据与采集数据，平台需回答：
1. 发电负荷是否可预测。
2. 可预测时长（如 1h/6h/24h）。
3. 供电需求是否可预测及可预测时长。
4. AGC 信号是否可提前感知。
5. 机组负荷的高相关因素。
6. 在统一平台中完成“数据-模型-业务”闭环管理。

## 3. 建设范围（RuoYi约束）
- 采用 `RuoYi-Vue`（前后端分离）作为基础框架。
- 复用 RuoYi 现有能力：登录鉴权、RBAC、操作日志、字典管理、参数配置、定时任务。
- 业务代码按 RuoYi 模块规范开发（Controller/Service/Mapper/Domain）。

## 4. 角色与权限
- 系统管理员：用户角色、菜单权限、参数配置、任务调度、模型发布审批。
- 数据分析员：数据导入、质量检查、特征分析、模型训练发起。
- 调度业务员：查看预测结果、告警处理、报表导出。

权限要求：
- 所有业务菜单必须配置权限标识（如 `power:predict:query`）。
- 高风险操作（删除数据、发布模型、手动重训）要求记录操作日志。

## 5. 功能需求

### FR-01 数据接入与管理
- 支持 CSV/Excel 导入（按模板校验字段）。
- 支持数据源台账：来源、时间范围、更新频率、责任人。
- 支持导入日志、失败记录回溯。

### FR-02 数据治理与质量监控
- 自动缺失值处理、异常检测、时间对齐。
- 质量看板：缺失率、异常率、重复率。
- 质量阈值可配置，超阈值触发告警。

### FR-03 分析与特征识别
- 时序趋势、季节性、波动分析。
- 相关性矩阵与高相关因素排序。
- 支持影响因子解释（如 SHAP 结果展示）。

### FR-04 模型训练与评估
- 支持至少两类模型对比（树模型 + 时序模型）。
- 输出 MAE/RMSE/MAPE/R2 等指标。
- 支持模型版本管理（训练时间、数据窗口、参数、指标）。

### FR-05 预测服务
- 提供负荷预测、需求预测、AGC 感知预测接口。
- 支持在线单次预测与定时批量预测。
- 返回预测值、误差评估、关键影响因素。

### FR-06 告警与处置
- 支持阈值告警、偏差告警、趋势突变告警。
- 告警支持状态流转：新建、已确认、已处理。
- 告警处理过程留痕。

### FR-07 报表与看板
- 首页看板：负荷趋势、预测对比、告警总览。
- 报表导出：日/周/月报（Excel 优先，PDF 可选）。

### FR-08 系统管理（复用RuoYi）
- 用户、角色、菜单、部门、字典、参数、通知公告。
- 定时任务用于：数据同步、模型重训、批量预测。

## 6. 非功能需求
- 性能：常规预测接口响应 <= 2s。
- 可用性：>= 99%。
- 安全性：JWT 鉴权、接口权限校验、操作审计。
- 可维护性：业务配置化、模型版本可追溯、异常可定位。

## 7. 数据需求
- 主键建议：`timestamp + station_id + unit_id`。
- 核心字段：时间、负荷、功率、气温、湿度、风速、AGC、机组状态。
- 标签字段：未来 t 时刻负荷/需求，AGC 目标值或等级标签。

## 8. 验收标准（RuoYi版）
1. 在 RuoYi 菜单中可完整访问“数据管理、分析建模、预测中心、告警中心”四大模块。
2. 菜单权限与按钮权限生效，越权访问被拦截。
3. 导入成功率 >= 98%，并可追溯失败原因。
4. 负荷预测验证集 MAPE <= 15%（可按老师要求调整）。
5. 定时任务可自动执行批量预测，结果可在看板展示。
6. 关键操作具备完整日志记录。

## 9. 约束与假设
- 默认使用 MySQL 作为业务数据库。
- 模型训练可由 Python 服务承担，RuoYi 负责业务编排与结果管理。
- 最终预测粒度与窗口以课程验收口径为准。

## 10. 五个核心问题的预测结论（基于当前采集数据初步验证）

数据样本说明（采集数据）：
- 时间范围：2021-07-20 11:03:00 至 2021-08-26 03:29:00（约 52827 分钟级样本）。
- 关键变量：机组实际负荷、机组负荷指令、AGC 控制指令、有功功率、负荷上下限、升降负荷率限制。

### Q1 发电负荷是否能预测？
- 结论：能预测。
- 依据：机组实际负荷序列连续性强，且与有功功率高度相关（相关系数约 0.9999），具备稳定时序预测基础。

### Q2 能预测多长时间之后的发电负荷？
- 初步建议窗口：
1. 短时高精度：10~30 分钟（NMAE 约 1.89%~4.03%）。
2. 可用预测：60 分钟（NMAE 约 6.44%）。
3. 中短时上限：120 分钟（NMAE 约 10.21%，精度明显下降）。
- 需求落地：平台默认提供 10/30/60/120 分钟四档预测。

### Q3 供电需求是否能预测？能预测多久之后的？
- 说明：以“机组负荷指令”作为供电需求代理变量。
- 结论：可预测。
- 初步建议窗口：
1. 10~30 分钟：NMAE 约 1.66%~3.67%。
2. 60 分钟：NMAE 约 5.91%。
3. 120 分钟：NMAE 约 9.33%（可用但需标注不确定性）。

### Q4 AGC 信号是否可以提前感知？
- 结论：可以提前感知。
- 依据：AGC 未来变化与当前负荷变化存在正相关（约 0.12~0.27）；用简单规则已能达到约 0.65~0.69 的 F1，说明存在可学习先行特征。
- 需求落地：将 AGC 任务定义为“未来 10/30/60 分钟变化预警（二分类）+ 未来值预测（回归）”双任务。

### Q5 哪些因素是机组负荷的高相关因素？
- 高相关因素（当前数据）：
1. 有功功率（power）：约 0.9999（极高相关）。
2. 机组负荷指令（load_cmd）：约 0.9920（高相关）。
3. AGC 控制指令（agc_cmd）：约 0.7942（中高相关）。
- 低信息量因素：
1. 负荷下限（load_low）在当前样本为常量（128），对建模贡献很低。
2. 负荷上限（load_up）几乎常量（330/330.05），主要用于约束而非解释。

## 11. 新增模型需求（补充）
- MR-01 负荷预测模型：输出未来 10/30/60/120 分钟机组实际负荷。
- MR-02 需求预测模型：输出未来 10/30/60/120 分钟机组负荷指令。
- MR-03 AGC 感知模型：输出未来 10/30/60 分钟 AGC 变化概率及风险等级。
- MR-04 因子解释：输出特征重要性排名与每次预测的主要影响因子。
- MR-05 置信信息：每次预测返回置信区间或风险标签（高/中/低）。

## 12. 新增验收指标（模型专项）
1. 负荷预测：60 分钟窗口 NMAE <= 8%（课程版目标）。
2. 需求预测：60 分钟窗口 NMAE <= 7%（课程版目标）。
3. AGC 感知：30 分钟窗口 F1 >= 0.70（课程版目标）。
4. 因子解释：前 10 个关键因子可视化展示且可导出。
